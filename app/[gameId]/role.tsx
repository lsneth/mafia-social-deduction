import { Text, View } from 'react-native'

export default function RoleScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Role</Text>
    </View>
  )
}
